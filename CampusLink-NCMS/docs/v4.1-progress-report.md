# CampusLink-NCMS V4.1 开发进度对照报告

本文档对照 2026-07-22 的 V4.1 基线。2026-09 的班级座位、强制准入、考勤使用记录与启动合并见 `docs/p12-stage-summary.md`。

生成时间: 2026-07-22 | 版本: v2.2 | 基线: `requirements-analysis.md` + `development-task-breakdown.md`

---

## 一、整体完成率

| 维度 | 已完成 | 总数 | 完成率 |
|------|--------|------|--------|
| 数据库迁移 | 15 | 15 | 100% |
| 教师端 API 端点 | 34 | 34 | 100% |
| 教师端 Web 页面 | 13 | 13 | 100% |
| 学生端 Agent 模块 | 18 | 18 | 100% |
| Protobuf 协议文件 | 4 | 4 | 100% |
| Crate 编译 (native + Windows) | 4 | 4 | 100% |
| P0-P10 执行轮次 | 10 | 10 | 100% |
| 部署脚本 (Service/防火墙/自启) | 6 | 6 | 100% |

---

## 二、已实现功能清单

### 数据库 (14 表)

| 表名 | 迁移 | 用途 |
|------|------|------|
| students | 0001 | 学生信息 |
| student_devices | 0002 | 设备注册 |
| system_configs | 0003 | 系统配置 (KV) |
| operation_logs | 0004 | 操作审计 |
| seed_system_configs | 0005 | 初始数据 |
| attendance_records | 0006 | 签到记录 |
| inspection_records | 0007 | 检查记录 |
| photos | 0008 | 检查照片 |
| hardware_snapshots | 0009 | 硬件快照 |
| hardware_changes | 0010 | 硬件变更记录 |
| process_guard_policies | 0011 | 进程守护策略 |
| admin_users | 0012 | 管理员账号 |
| device_whitelist | 0013 | 设备白名单 |
| repair_orders | 0014 | 维修工单 |
| network_accounts | 0015 | 网络认证账号 |

### 教师端 API (28 端点)

**公开路由** (无需认证):
- `GET /api/health` — 健康检查
- `POST /api/auth/login` — JWT 登录
- `POST /api/devices/register` — 设备注册
- `GET /ws` — WebSocket 连接

**受保护路由** (需 Bearer Token):
- `GET /api/dashboard/overview` — 仪表盘统计
- `GET /api/devices`、`POST /api/devices/:id/mode`、`POST /api/devices/:id/lock`、`POST /api/devices/:id/unlock`
- `GET/POST /api/attendance`、`GET /api/attendance/statistics`、`POST /api/attendance/retroactive`、`GET /api/attendance/export`
- `GET/POST /api/inspection`、`GET /api/alerts`、`POST /api/alerts/:id/resolve`、`GET /api/inspection/export`
- `POST /api/photos/upload`、`GET /api/photos`、`GET /api/photos/:id`
- `POST/GET /api/hardware/snapshot`、`GET /api/hardware/changes`
- `GET /api/logs`、`GET /api/logs/export`
- `POST/GET/DELETE /api/policies/process-guard`
- `GET/POST/PUT/DELETE /api/students`、`POST /api/students/import`、`GET /api/students/export`
- `GET /api/settings/lock-password`、`PUT /api/settings/lock-password`、`GET /api/settings/schedule`、`PUT /api/settings/schedule`
- `GET /api/devices/whitelist`、`POST /api/devices/whitelist/import`、`POST /api/devices/whitelist/:id/approve`、`DELETE /api/devices/whitelist/:id`
- `POST/GET/PUT/DELETE /api/repair-orders`
- `POST/GET/PUT/DELETE /api/network-accounts`

### 教师端 Web (12 页面)

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录 | `/login` | JWT + token localStorage + 401 自动跳转 |
| 仪表盘 | `/dashboard` | 设备数/在线/模式/签到/告警统计 |
| 设备管理 | `/devices` | 列表 + 模式切换 + 锁屏/解锁 |
| 监控总览 | `/monitor` | 在线设备表格 + 15s 轮询 |
| 签到管理 | `/attendance` | 列表 + 统计卡片 + 补签弹窗 |
| 检查告警 | `/alerts` | 检查记录 + 告警处理 + 图片上传 |
| 硬件快照 | `/hardware` | 详情 + 变更记录表 |
| 日志中心 | `/logs` | 分页查询 + 类型筛选 |
| 学生管理 | `/students` | CRUD + Excel 导入导出 |
| 系统设置 | `/settings` | 锁屏密码修改 |
| 维修工单 | `/repairs` | 创建/列表/状态流转 (pending→processing→completed) |
| 设备白名单 | `/device-whitelist` | Excel 导入 + 审批 |
| 网络认证账号 | `/network-accounts` | 外网认证账号 CRUD + 自动登录/注销 |

**前端基础设施**: axios 共享实例 (`api/http.ts`)、自动 Bearer Token 注入、401 死循环防护

### 学生端 Agent (3 进程 x 18 模块)

**agent-core**: 信息采集、设备注册、配置持久化 (config.enc AES256)、WebSocket 30s 心跳、Binary 消息加解密、签到/检查、硬件采集 (sysinfo + WMIC 外设)、模式切换联动锁屏、UDP 广播发现、指纹双向校验、进程守护策略同步、campus-guard 存活监控、锁屏进程退出检测

**campus-guard**: 双进程守护 (agent-core + campus-lock), 异常退出自动拉起 (上限 10 次)

**campus-lock**: 全屏 eframe GUI、超级密码解锁、失败计数限流、WH_KEYBOARD_LL 键盘钩子 (Win/Alt+Tab/Alt+F4/Ctrl+Esc 屏蔽)、IME 修复

### 安全通信

- JWT: HS256, 8h 有效期, admin_users 表, bcrypt (cost 4)
- WebSocket 加密: AES256-GCM, 12 字节 nonce + ciphertext, Message::Binary, session_key 协商
- 本地配置: config.enc AES256-GCM, PBKDF2 密钥派生

### 部署运维

- `install.bat`: 管理员检测 + 一键全装 (Service + 防火墙 + 计划任务)
- `uninstall.bat`: 统一卸载 (保留数据)
- `teacher-server/install-service.bat`: sc create (开机自启 + 崩溃重启)
- `teacher-server/install-firewall.bat`: netsh TCP 8080 放行
- `student-agent/install-autostart.bat`: schtasks onlogon
- `teacher-server/generate-cert.bat`: HTTPS 自签名证书 (OpenSSL/PowerShell)
- 配置路径 exe-relative 自动解析 (支持 Windows Service 环境)
- `docs/deployment-guide.md`: 拓扑/安装/运维/故障排查/安全

---

## 三、缺口清单 (已全部补齐)

> 提交 `c07fba2` 后全部 11 项缺口已实现，后端编译通过，API 端到端验证全部返回 200。

### 已补齐 (11 项)

| # | 功能 | 状态 | 变更要点 |
|---|------|------|---------|
| 1 | **网络认证账号 (network_accounts)** | 已实现 | 迁移 0015 + domain/network_account.rs + API handlers + NetworkAccounts.vue + 路由/菜单 |
| 2 | **座位图可视化** | 已实现 | Monitor.vue 新增网格视图 (列表/座位图切换) |
| 3 | **前端 WebSocket 实时推送** | 已实现 | composables/useWebSocket.ts + Layout 集成 + 告警弹窗 |
| 4 | **Protobuf WS 消息编码** | 已实现 | src/proto/mod.rs + handlers.rs protobuf 编解码 |
| 5 | **SQLCipher 数据库加密** | 已实现 | Cargo.toml feature flag + database.rs 条件编译 PRAGMA key |
| 6 | **签到记录导出 (CSV)** | 已实现 | GET /api/attendance/export → text/csv |
| 7 | **检查日志导出 (CSV)** | 已实现 | GET /api/inspection/export → text/csv |
| 8 | **模式调度前端配置** | 已实现 | Settings.vue 定时切换卡片 + GET/PUT /api/settings/schedule |
| 9 | **Dashboard 统计增强** | 已实现 | 新增 pendingRepairCount + pendingWhitelistCount |
| 10 | **图片上传压缩** | 已实现 | Alerts.vue Canvas 压缩 (1200px, 0.7q) |
| 11 | **键盘/鼠标检查类型** | 已实现 | Alerts.vue dropdown 新增 keyboard/mouse 选项 |

### 待验证 (1 项)

| # | 功能 | 状态 |
|---|------|------|
| 13 | Windows 实机 Service/计划任务联调 | 脚本已就绪，未在 Windows 端到端验证 |

---

## 四、并发压测结果 (120 设备)

> 提交 `887026d`，测试环境与教师端服务同机部署。

| 指标 | 结果 |
|------|------|
| HTTP 注册成功率 | 120/120 (100%) |
| WebSocket 连接成功率 | 120/120 (100%) |
| WS 连接延迟 (平均) | 10.9ms |
| 心跳 ACK 成功率 | 1440/1440 (100%) |
| 稳定 QPS | 24 hb/sec (持续 60s) |
| 结论 | **通过 — 支持 100+ 并发设备** |

---

## 四、里程碑判定

| 里程碑 | 定义 | 达成? |
|--------|------|-------|
| M1 可启动骨架 | 三端独立启动 + Proto 可生成 | 达成 |
| M2 最小业务闭环 | 注册 + 在线状态 + 模式切换 + 签到 | 达成 |
| M3 课堂管理闭环 | 检查流程 + 警告 + 日志查询 + 锁屏 | 达成 |
| M4 企业级能力闭环 | 网络认证 + 双进程守护 + 锁屏 + 部署方案 | 达成 |

---

## 五、当前状态总结

核心功能与所有需求文档中的功能域 **100% 覆盖**：
- 数据库 15 张表全部落地
- 教师端 API 34 个端点全部实现
- 教师端 Web 13 个页面全部可用
- 学生端 Agent 3 进程 18 模块全部到位
- 部署运维层完整 (Service/防火墙/计划任务/安装器/部署文档)
- 安全通信完整 (JWT + AES256-GCM WS 加密 + 配置加密)
- Protobuf 协议就绪 (proto 编译链 + WS 消息编解码)
- SQLCipher 编译时特性就绪 (`cargo build --features sqlcipher`)

**剩余待办** (验收阶段):
- Windows 实机 Service/计划任务 + 签到/检查/告警/图片全链路联调
- Inno Setup 安装器替代 batch scripts (可选)
